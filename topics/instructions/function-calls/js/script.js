/**
 * Function Calls
 * Matteo Edmonds-Tiano
 * 
 * Working on the function calls examples.
 */

"use strict";


function setup() {
    // Once at the beginning of the program
    createCanvas(640,480);
}


function draw() {
    // Every frame(essentially as long as its up you'll see it!)
    background(255, 100, 100);
    rect(200, 80, 240, 320); 
    // First number is the x, Second is the y, and the 3rd and 4th are the width and height
    

}